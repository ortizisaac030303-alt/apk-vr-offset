<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vrcontroller.offsetapp"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Minimum and target SDK versions -->
    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="34" />

    <!-- Required permissions for VR -->
    <uses-permission android:name="android.permission.VR_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Hand tracking permission -->
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />

    <!-- VR mode requirement -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="false" />
    <uses-feature
        android:name="android.software.vr.mode"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:debuggable="false">

        <!-- Main activity -->
        <activity
            android:name="com.unity3d.player.UnityPlayerActivity"
            android:screenOrientation="landscape"
            android:configChanges="fontScale|orientation|keyboardHidden|screenSize|smallestScreenSize|layoutDirection"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="example.com" />
            </intent-filter>
        </activity>

        <!-- Handles app being killed -->
        <activity
            android:name="com.unity3d.player.UnityPlayerServiceActivity"
            android:exported="false" />

    </application>

</manifest>
